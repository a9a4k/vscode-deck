diff --git a/package.json b/package.json
index bb7672c..b8cb239 100644
--- a/package.json
+++ b/package.json
@@ -245,9 +245,14 @@
       },
       {
         "command": "deck.runLauncher",
-        "title": "Run Terminal Launcher",
+        "title": "Open…",
         "icon": "$(play)"
       },
+      {
+        "command": "deck.addBookmark",
+        "title": "Add Bookmark…",
+        "icon": "$(globe)"
+      },
       {
         "command": "deck.openTerminal",
         "title": "Open Terminal"
@@ -320,31 +325,31 @@
           "when": "view == deck.repositories && viewItem == deck.repository",
           "group": "inline"
         },
-        {
-          "command": "deck.addTerminal",
-          "when": "view == deck.repositories && (viewItem == deck.worktree || viewItem == deck.worktree.active || viewItem == deck.worktree.main || viewItem == deck.worktree.main.active) && deck.tmuxAvailable",
-          "group": "inline"
-        },
         {
           "command": "deck.runLauncher",
           "when": "view == deck.repositories && (viewItem == deck.worktree || viewItem == deck.worktree.active || viewItem == deck.worktree.main || viewItem == deck.worktree.main.active) && deck.tmuxAvailable",
           "group": "inline@5"
         },
+        {
+          "command": "deck.addBookmark",
+          "when": "view == deck.repositories && (viewItem == deck.worktree || viewItem == deck.worktree.active || viewItem == deck.worktree.main || viewItem == deck.worktree.main.active)",
+          "group": "inline@6"
+        },
         {
           "command": "deck.switchWorktree",
           "when": "view == deck.repositories && (viewItem == deck.worktree || viewItem == deck.worktree.main)",
           "group": "navigation@1"
         },
-        {
-          "command": "deck.removeWorktree",
-          "when": "view == deck.repositories && (viewItem == deck.worktree || viewItem == deck.worktree.active)",
-          "group": "navigation@3"
-        },
         {
           "command": "deck.openWorktreeInNewWindow",
           "when": "view == deck.repositories && (viewItem == deck.worktree || viewItem == deck.worktree.main)",
           "group": "navigation@2"
         },
+        {
+          "command": "deck.removeWorktree",
+          "when": "view == deck.repositories && (viewItem == deck.worktree || viewItem == deck.worktree.active)",
+          "group": "navigation@3"
+        },
         {
           "command": "deck.removeRepository",
           "when": "view == deck.repositories && viewItem == deck.repository",
diff --git a/src/extension.ts b/src/extension.ts
index e0d8339..da02bc5 100644
--- a/src/extension.ts
+++ b/src/extension.ts
@@ -551,6 +551,16 @@ export async function activate(context: vscode.ExtensionContext): Promise<void>
     vscode.commands.registerCommand('deck.addWorktree', (node) => addWorktree.run(node)),
     vscode.commands.registerCommand('deck.addTerminal', (node) => addTerminal.run(node)),
     vscode.commands.registerCommand('deck.runLauncher', (node) => runLauncher.run(node)),
+    // PROTOTYPE (prototypes/bookmark-row-actions): throwaway stand-in for the
+    // real Add Bookmark flow — no store, no rows. Entry-point placement only.
+    vscode.commands.registerCommand('deck.addBookmark', async () => {
+      const url = await vscode.window.showInputBox({
+        prompt: 'Add Bookmark',
+        placeHolder: 'https://…',
+      });
+      if (!url) return;
+      void vscode.window.showInformationMessage(`Bookmark added: ${url}`);
+    }),
     vscode.commands.registerCommand('deck.openTerminal', (node) => openTerminal.run(node)),
     vscode.commands.registerCommand('deck.openTerminalInNewWindow', (node) =>
       openTerminalInNewWindow.run(node),
diff --git a/src/terminal/runLauncherCommand.ts b/src/terminal/runLauncherCommand.ts
index 15da54e..c850f13 100644
--- a/src/terminal/runLauncherCommand.ts
+++ b/src/terminal/runLauncherCommand.ts
@@ -19,6 +19,8 @@ interface RunLauncherTmuxCli extends AddTerminalTmuxCli {
 type LauncherQuickPickItem = vscode.QuickPickItem & {
   launcher?: TerminalLauncher;
   configure?: true;
+  newTerminal?: true;
+  addBookmark?: true;
 };
 
 interface RunLauncherCommandOptions {
@@ -66,13 +68,23 @@ export class RunLauncherCommand {
     const repositoryLaunchers = vscode.workspace.getConfiguration('deck').get('repositoryLaunchers', []);
     const groups = await this.resolveLaunchers(node.worktree.path, userLaunchers, repositoryLaunchers);
     const picked = await vscode.window.showQuickPick(toQuickPickItems(groups), {
-      placeHolder: 'Run Terminal Launcher',
+      placeHolder: 'New Terminal, run a launcher, or add a Bookmark',
     });
     if (!picked) return;
     if (picked.configure) {
       await vscode.commands.executeCommand('workbench.action.openSettings', 'deck.repositoryLaunchers');
       return;
     }
+    if (picked.addBookmark) {
+      await vscode.commands.executeCommand('deck.addBookmark');
+      return;
+    }
+    if (picked.newTerminal) {
+      await this.beforeCreate();
+      await createAndOpenTerminal(this.tmux, node, this.sessionUriCodec, this.focusTerminal);
+      this.wakePoll();
+      return;
+    }
     if (!picked.launcher) return;
 
     await this.beforeCreate();
@@ -88,14 +100,28 @@ export class RunLauncherCommand {
 }
 
 function toQuickPickItems(groups: LauncherGroups): LauncherQuickPickItem[] {
+  const newTerminal: LauncherQuickPickItem = { label: '$(add) New Terminal', newTerminal: true };
+  const bookmark: LauncherQuickPickItem[] = [
+    { kind: vscode.QuickPickItemKind.Separator, label: 'Bookmark' },
+    { label: '$(globe) Add Bookmark…', addBookmark: true },
+  ];
+
   if (!hasLaunchers(groups)) {
-    return [{ label: 'No launchers configured — Configure…', configure: true }];
+    return [
+      { kind: vscode.QuickPickItemKind.Separator, label: 'Terminal' },
+      newTerminal,
+      { label: 'No launchers configured — Configure…', configure: true },
+      ...bookmark,
+    ];
   }
 
   return [
+    { kind: vscode.QuickPickItemKind.Separator, label: 'Terminal' },
+    newTerminal,
     ...groupItems('This repository (shared)', groups.repo),
     ...groupItems('This repository (personal)', groups.repositoryLocal),
     ...groupItems('User', groups.user),
+    ...bookmark,
   ];
 }
 
